<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PROOF OF VOID | Уничтожение капитала</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Courier New', monospace;
            background: linear-gradient(to bottom, #000000, #0a0a0a, #000000);
            color: #fff;
            min-height: 100vh;
            overflow-x: hidden;
            position: relative;
        }

        /* Анимированный фон с частицами */
        .particles {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            overflow: hidden;
            z-index: 0;
            pointer-events: none;
        }

        .particle {
            position: absolute;
            width: 2px;
            height: 2px;
            background: rgba(255, 255, 255, 0.3);
            animation: fall linear infinite;
        }

        @keyframes fall {
            0% {
                transform: translateY(-100px);
                opacity: 0;
            }
            50% {
                opacity: 1;
            }
            100% {
                transform: translateY(100vh);
                opacity: 0;
            }
        }

        .container {
            max-width: 900px;
            margin: 0 auto;
            padding: 20px;
            position: relative;
            z-index: 1;
        }

        /* Заголовок */
        .header {
            text-align: center;
            padding: 60px 20px 40px;
            position: relative;
        }

        .skull {
            font-size: 80px;
            animation: pulse 2s ease-in-out infinite;
            display: inline-block;
        }

        @keyframes pulse {
            0%, 100% { transform: scale(1); opacity: 0.8; }
            50% { transform: scale(1.1); opacity: 1; }
        }

        .title {
            font-size: 48px;
            font-weight: bold;
            margin: 20px 0;
            letter-spacing: 8px;
            text-shadow: 0 0 20px rgba(255, 0, 0, 0.5);
            animation: glitch 3s infinite;
        }

        @keyframes glitch {
            0%, 90%, 100% { transform: translate(0); }
            92% { transform: translate(-2px, 2px); }
            94% { transform: translate(2px, -2px); }
            96% { transform: translate(-2px, -2px); }
        }

        .subtitle {
            font-size: 16px;
            opacity: 0.7;
            letter-spacing: 3px;
            margin-bottom: 40px;
        }

        /* Главный счетчик */
        .main-counter {
            background: linear-gradient(135deg, #1a0000, #000000);
            border: 2px solid #ff0000;
            border-radius: 15px;
            padding: 40px;
            margin: 40px 0;
            text-align: center;
            box-shadow: 0 0 40px rgba(255, 0, 0, 0.3);
            position: relative;
            overflow: hidden;
        }

        .main-counter::before {
            content: '🔥';
            position: absolute;
            top: -20px;
            left: -20px;
            font-size: 100px;
            opacity: 0.05;
            transform: rotate(-15deg);
        }

        .main-counter::after {
            content: '🔥';
            position: absolute;
            bottom: -20px;
            right: -20px;
            font-size: 100px;
            opacity: 0.05;
            transform: rotate(15deg);
        }

        .counter-label {
            font-size: 14px;
            opacity: 0.6;
            letter-spacing: 2px;
            margin-bottom: 15px;
        }

        .counter-value {
            font-size: 56px;
            font-weight: bold;
            color: #ff3333;
            text-shadow: 0 0 20px rgba(255, 51, 51, 0.5);
            margin: 10px 0;
        }

        .counter-usd {
            font-size: 24px;
            opacity: 0.5;
            margin-top: 10px;
        }

        /* Статистика */
        .stats {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin: 40px 0;
        }

        .stat-card {
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 10px;
            padding: 25px;
            text-align: center;
            transition: all 0.3s;
        }

        .stat-card:hover {
            background: rgba(255, 255, 255, 0.05);
            border-color: rgba(255, 0, 0, 0.3);
            transform: translateY(-5px);
        }

        .stat-icon {
            font-size: 40px;
            margin-bottom: 15px;
        }

        .stat-value {
            font-size: 32px;
            font-weight: bold;
            color: #ff6666;
            margin: 10px 0;
        }

        .stat-label {
            font-size: 12px;
            opacity: 0.6;
            letter-spacing: 1px;
        }

        /* Форма уничтожения */
        .void-form {
            background: linear-gradient(135deg, #0a0000, #000000);
            border: 2px solid #ff0000;
            border-radius: 15px;
            padding: 40px;
            margin: 40px 0;
            box-shadow: 0 0 40px rgba(255, 0, 0, 0.2);
        }

        .form-title {
            font-size: 24px;
            text-align: center;
            margin-bottom: 30px;
            letter-spacing: 3px;
        }

        .input-group {
            margin-bottom: 25px;
        }

        .input-label {
            display: block;
            font-size: 12px;
            opacity: 0.7;
            letter-spacing: 2px;
            margin-bottom: 10px;
        }

        .input-field {
            width: 100%;
            padding: 15px;
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.2);
            border-radius: 8px;
            color: #fff;
            font-family: 'Courier New', monospace;
            font-size: 16px;
            transition: all 0.3s;
        }

        .input-field:focus {
            outline: none;
            border-color: #ff0000;
            background: rgba(255, 255, 255, 0.08);
            box-shadow: 0 0 20px rgba(255, 0, 0, 0.2);
        }

        .warning-box {
            background: rgba(255, 0, 0, 0.1);
            border: 1px solid rgba(255, 0, 0, 0.3);
            border-radius: 8px;
            padding: 20px;
            margin: 25px 0;
        }

        .warning-box .icon {
            font-size: 24px;
            margin-bottom: 10px;
        }

        .warning-text {
            font-size: 14px;
            line-height: 1.6;
            opacity: 0.9;
        }

        .checkbox-group {
            display: flex;
            align-items: center;
            margin: 25px 0;
            cursor: pointer;
        }

        .checkbox-group input[type="checkbox"] {
            width: 20px;
            height: 20px;
            margin-right: 15px;
            cursor: pointer;
        }

        .checkbox-label {
            font-size: 14px;
            line-height: 1.5;
        }

        .destroy-button {
            width: 100%;
            padding: 20px;
            background: linear-gradient(135deg, #ff0000, #cc0000);
            border: none;
            border-radius: 10px;
            color: #fff;
            font-size: 18px;
            font-weight: bold;
            letter-spacing: 3px;
            cursor: pointer;
            transition: all 0.3s;
            text-transform: uppercase;
            box-shadow: 0 5px 20px rgba(255, 0, 0, 0.3);
        }

        .destroy-button:hover {
            background: linear-gradient(135deg, #cc0000, #990000);
            transform: translateY(-2px);
            box-shadow: 0 8px 30px rgba(255, 0, 0, 0.5);
        }

        .destroy-button:active {
            transform: translateY(0);
        }

        .destroy-button:disabled {
            background: #333;
            cursor: not-allowed;
            opacity: 0.5;
        }

        /* Лента жертвоприношений */
        .sacrifices {
            margin: 60px 0;
        }

        .sacrifices-title {
            font-size: 20px;
            text-align: center;
            margin-bottom: 30px;
            letter-spacing: 3px;
            opacity: 0.8;
        }

        .sacrifice-item {
            background: rgba(255, 255, 255, 0.02);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 10px;
            padding: 20px;
            margin-bottom: 15px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            transition: all 0.3s;
            animation: fadeIn 0.5s ease-in;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .sacrifice-item:hover {
            background: rgba(255, 255, 255, 0.05);
            border-color: rgba(255, 0, 0, 0.2);
        }

        .sacrifice-address {
            font-size: 14px;
            color: #ff6666;
            font-family: monospace;
        }

        .sacrifice-amount {
            font-size: 18px;
            font-weight: bold;
            color: #ff3333;
        }

        .sacrifice-message {
            font-size: 12px;
            opacity: 0.6;
            font-style: italic;
            margin-top: 8px;
        }

        .sacrifice-time {
            font-size: 11px;
            opacity: 0.4;
            margin-top: 5px;
        }

        /* Философский текст */
        .manifesto {
            margin: 60px 0;
            padding: 40px;
            text-align: center;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }

        .manifesto-title {
            font-size: 28px;
            margin-bottom: 25px;
            letter-spacing: 4px;
        }

        .manifesto-text {
            font-size: 14px;
            line-height: 2;
            opacity: 0.7;
            max-width: 700px;
            margin: 0 auto 20px;
        }

        /* Ссылки */
        .links {
            text-align: center;
            margin: 40px 0;
            padding: 30px;
        }

        .link-button {
            display: inline-block;
            padding: 12px 30px;
            margin: 10px;
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.2);
            border-radius: 8px;
            color: #fff;
            text-decoration: none;
            transition: all 0.3s;
            letter-spacing: 2px;
        }

        .link-button:hover {
            background: rgba(255, 0, 0, 0.1);
            border-color: #ff0000;
        }

        /* Футер */
        .footer {
            text-align: center;
            padding: 40px 20px;
            opacity: 0.4;
            font-size: 12px;
            letter-spacing: 2px;
        }

        /* Адаптив */
        @media (max-width: 768px) {
            .title {
                font-size: 32px;
                letter-spacing: 4px;
            }

            .counter-value {
                font-size: 40px;
            }

            .skull {
                font-size: 60px;
            }

            .stats {
                grid-template-columns: 1fr;
            }
        }

        /* Демо уведомление */
        .demo-notice {
            background: rgba(255, 165, 0, 0.1);
            border: 1px solid rgba(255, 165, 0, 0.3);
            border-radius: 10px;
            padding: 20px;
            margin: 20px 0;
            text-align: center;
        }

        .demo-notice-text {
            font-size: 14px;
            color: #ffaa00;
        }
    </style>
</head>
<body>
    <!-- Анимированные частицы -->
    <div class="particles" id="particles"></div>

    <div class="container">
        <!-- Заголовок -->
        <div class="header">
            <div class="skull">💀</div>
            <h1 class="title">PROOF OF VOID</h1>
            <p class="subtitle">УНИЧТОЖЕНИЕ КАПИТАЛА</p>
        </div>

        <!-- Демо уведомление -->
        <div class="demo-notice">
            <div class="demo-notice-text">
                ⚠️ ЭТО ДЕМО-ВЕРСИЯ | Для реальной работы требуется интеграция с блокчейном
            </div>
        </div>

        <!-- Главный счетчик -->
        <div class="main-counter">
            <div class="counter-label">🔥 УНИЧТОЖЕНО НАВСЕГДА 🔥</div>
            <div class="counter-value" id="totalVoided">12.47 ETH</div>
            <div class="counter-usd">≈ $29,847 USD</div>
        </div>

        <!-- Статистика -->
        <div class="stats">
            <div class="stat-card">
                <div class="stat-icon">👥</div>
                <div class="stat-value" id="participants">247</div>
                <div class="stat-label">УЧАСТНИКОВ</div>
            </div>
            <div class="stat-card">
                <div class="stat-icon">⏱️</div>
                <div class="stat-value" id="lastSacrifice">3 мин</div>
                <div class="stat-label">НАЗАД</div>
            </div>
            <div class="stat-card">
                <div class="stat-icon">📈</div>
                <div class="stat-value" id="avgAmount">0.05 ETH</div>
                <div class="stat-label">СРЕДНЯЯ СУММА</div>
            </div>
        </div>

        <!-- Форма уничтожения -->
        <div class="void-form">
            <h2 class="form-title">ОТПРАВИТЬ В ПУСТОТУ</h2>

            <div class="input-group">
                <label class="input-label">СУММА (ETH)</label>
                <input type="number" class="input-field" id="amount" placeholder="0.1" step="0.01" min="0.001">
            </div>

            <div class="input-group">
                <label class="input-label">СООБЩЕНИЕ (ОПЦИОНАЛЬНО)</label>
                <input type="text" class="input-field" id="message" placeholder="Почему вы это делаете?" maxlength="100">
            </div>

            <div class="warning-box">
                <div class="icon">⚠️</div>
                <div class="warning-text">
                    <strong>ВНИМАНИЕ: ЭТО НЕОБРАТИМО</strong><br>
                    Деньги будут отправлены на burn address и уничтожены навсегда.<br>
                    Они не достанутся никому. Никогда.<br>
                    Возврат невозможен.
                </div>
            </div>

            <label class="checkbox-group">
                <input type="checkbox" id="confirmCheck">
                <span class="checkbox-label">
                    Я понимаю, что деньги будут уничтожены навсегда и безвозвратно. 
                    Это не инвестиция. Это акт уничтожения капитала.
                </span>
            </label>

            <button class="destroy-button" id="destroyBtn" disabled>
                🔥 УНИЧТОЖИТЬ 🔥
            </button>
        </div>

        <!-- Лента жертвоприношений -->
        <div class="sacrifices">
            <h3 class="sacrifices-title">━━━ ПОСЛЕДНИЕ ЖЕРТВОПРИНОШЕНИЯ ━━━</h3>
            
            <div class="sacrifice-item">
                <div>
                    <div class="sacrifice-address">0x7a3f...2b9c</div>
                    <div class="sacrifice-message">"За абсурд капитализма"</div>
                    <div class="sacrifice-time">2 минуты назад</div>
                </div>
                <div class="sacrifice-amount">0.1 ETH</div>
            </div>

            <div class="sacrifice-item">
                <div>
                    <div class="sacrifice-address">0x9b2c...4f7d</div>
                    <div class="sacrifice-message">"Почему нет?"</div>
                    <div class="sacrifice-time">15 минут назад</div>
                </div>
                <div class="sacrifice-amount">0.05 ETH</div>
            </div>

            <div class="sacrifice-item">
                <div>
                    <div class="sacrifice-address">0x4d8e...a1c3</div>
                    <div class="sacrifice-message"></div>
                    <div class="sacrifice-time">27 минут назад</div>
                </div>
                <div class="sacrifice-amount">0.2 ETH</div>
            </div>

            <div class="sacrifice-item">
                <div>
                    <div class="sacrifice-address">0x2f5a...8d6b</div>
                    <div class="sacrifice-message">"Освобождение от системы"</div>
                    <div class="sacrifice-time">1 час назад</div>
                </div>
                <div class="sacrifice-amount">0.15 ETH</div>
            </div>

            <div class="sacrifice-item">
                <div>
                    <div class="sacrifice-address">0x8c1d...3e9f</div>
                    <div class="sacrifice-message">"Деньги - это иллюзия"</div>
                    <div class="sacrifice-time">2 часа назад</div>
                </div>
                <div class="sacrifice-amount">0.08 ETH</div>
            </div>
        </div>

        <!-- Манифест -->
        <div class="manifesto">
            <h2 class="manifesto-title">МАНИФЕСТ</h2>
            <p class="manifesto-text">
                В эпоху, когда деньги существуют как цифры в базах данных,
                их уничтожение становится актом сопротивления системе 
                бесконечного накопления.
            </p>
            <p class="manifesto-text">
                Банки используют ваши деньги для создания новых денег.
                Здесь деньги исчезают по-настоящему.
            </p>
            <p class="manifesto-text">
                Это не протест. Это констатация абсурда.<br>
                Это не благотворительность. Никто не получит выгоды.<br>
                Это коллективный акт освобождения от иллюзии ценности.
            </p>
            <p class="manifesto-text">
                Каждый перевод — голос против системы.<br>
                Каждый сожженный токен — памятник бессмысленности.
            </p>
            <p class="manifesto-text" style="font-size: 18px; margin-top: 30px; opacity: 1;">
                <strong>Присоединяйтесь к Пустоте.</strong>
            </p>
        </div>

        <!-- Ссылки -->
        <div class="links">
            <a href="#" class="link-button">📊 ETHERSCAN</a>
            <a href="#" class="link-button">📖 ДОКУМЕНТАЦИЯ</a>
            <a href="#" class="link-button">💬 КОМЬЮНИТИ</a>
        </div>

        <!-- Футер -->
        <div class="footer">
            PROOF OF VOID © 2025 | КОНЦЕПТУАЛЬНЫЙ АРТ-ПРОЕКТ<br>
            0x000000000000000000000000000000000000dEaD
        </div>
    </div>

    <script>
        // Генерация анимированных частиц
        function createParticles() {
            const particlesContainer = document.getElementById('particles');
            const particleCount = 50;

            for (let i = 0; i < particleCount; i++) {
                const particle = document.createElement('div');
                particle.className = 'particle';
                particle.style.left = Math.random() * 100 + '%';
                particle.style.animationDuration = (Math.random() * 3 + 2) + 's';
                particle.style.animationDelay = Math.random() * 5 + 's';
                particlesContainer.appendChild(particle);
            }
        }

        // Активация кнопки при подтверждении
        document.getElementById('confirmCheck').addEventListener('change', function() {
            document.getElementById('destroyBtn').disabled = !this.checked;
        });

        // Обработка нажатия кнопки уничтожения (демо)
        document.getElementById('destroyBtn').addEventListener('click', function() {
            const amount = document.getElementById('amount').value;
            const message = document.getElementById('message').value;

            if (!amount || amount <= 0) {
                alert('⚠️ Укажите сумму для уничтожения');
                return;
            }

            // Демо-анимация
            this.textContent = '🔥 УНИЧТОЖЕНИЕ... 🔥';
            this.disabled = true;

            setTimeout(() => {
                alert(`✅ ДЕМО: ${amount} ETH были бы уничтожены\n\nСообщение: "${message || 'нет'}"\n\n⚠️ Это демо-версия. Для реальной работы требуется подключение к Web3 и смарт-контракту.`);
                
                this.textContent = '🔥 УНИЧТОЖИТЬ 🔥';
                document.getElementById('confirmCheck').checked = false;
                document.getElementById('amount').value = '';
                document.getElementById('message').value = '';
            }, 2000);
        });

        // Анимация счетчика (демо)
        function animateCounter() {
            const counter = document.getElementById('totalVoided');
            let currentValue = 12.47;
            
            setInterval(() => {
                currentValue += Math.random() * 0.01;
                counter.textContent = currentValue.toFixed(2) + ' ETH';
            }, 5000);
        }

        // Инициализация
        window.addEventListener('load', () => {
            createParticles();
            animateCounter();
        });

        // Пасхалка: Konami Code
        let konamiCode = [];
        const konamiSequence = ['ArrowUp', 'ArrowUp', 'ArrowDown', 'ArrowDown', 'ArrowLeft', 'ArrowRight', 'ArrowLeft', 'ArrowRight', 'b', 'a'];

        document.addEventListener('keydown', (e) => {
            konamiCode.push(e.key);
            konamiCode = konamiCode.slice(-10);

            if (konamiCode.join(',') === konamiSequence.join(',')) {
                document.body.style.animation = 'glitch 0.3s infinite';
                setTimeout(() => {
                    document.body.style.animation = '';
                    alert('🎮 Achievement Unlocked: You found the void within the void');
                }, 3000);
            }
        });
    </script>
</body>
</html>
